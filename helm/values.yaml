# Default values for helm.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1
image:
  registry: "ghcr.io"
  repository: "k8s-k3s-385712"
  # Overrides the image tag whose default is the chart appVersion.
  os: linux
  tag: "v.1.0.7-9b2ade5"
  arch: amd64
secret:
  name: "kbot"
  env: "TELE_TOKEN"
  key: "token"
securityContext:
  privileged: true
